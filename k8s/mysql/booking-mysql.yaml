apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: booking-mysql-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Secret
metadata:
  name: booking-mysql-secret
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: rootpassword
  MYSQL_DATABASE: auth_db
  MYSQL_USER: auth_user
  MYSQL_PASSWORD: authpass
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: booking-mysql
spec:
  replicas: 1
  selector:
    matchLabels:
      app: booking-mysql
  template:
    metadata:
      labels:
        app: booking-mysql
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          ports:
            - containerPort: 3306
          envFrom:
            - secretRef:
                name: booking-mysql-secret
          volumeMounts:
            - mountPath: /var/lib/mysql
              name: mysql-storage
            - mountPath: /docker-entrypoint-initdb.d
              name: initdb
              readOnly: true
      volumes:
        - name: mysql-storage
          persistentVolumeClaim:
            claimName: booking-mysql-pvc
        - name: initdb
          hostPath:
            path: ./booking-service/db
---
apiVersion: v1
kind: Service
metadata:
  name: booking-mysql
spec:
  ports:
    - port: 3306
      targetPort: 3306
  selector:
    app: booking-mysql
