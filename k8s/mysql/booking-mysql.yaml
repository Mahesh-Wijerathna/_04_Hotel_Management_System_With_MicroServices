apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: booking-mysql-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: booking-mysql
spec:
  selector:
    matchLabels:
      app: booking-mysql
  serviceName: "booking-mysql"
  replicas: 1
  template:
    metadata:
      labels:
        app: booking-mysql
    spec:
      containers:
      - name: booking-mysql
        image: mysql:8.0
        ports:
        - containerPort: 3306
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: booking-mysql-secret
              key: root-password
        - name: MYSQL_DATABASE
          value: booking_db
        - name: MYSQL_USER
          value: booking_user
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: booking-mysql-secret
              key: user-password
        volumeMounts:
        - name: booking-mysql-data
          mountPath: /var/lib/mysql
  volumeClaimTemplates:
  - metadata:
      name: booking-mysql-data
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 5Gi
---
apiVersion: v1
kind: Service
metadata:
  name: booking-mysql
spec:
  ports:
  - port: 3306
    targetPort: 3306
  clusterIP: None
  selector:
    app: booking-mysql
